# `chrnio`'s Dotfiles

```console
chrn@archbox:~$ fetch

os        arch linux
wm        hyprland
editor    neovim
shell     zsh
terminal  kitty
theme     monochrome
```

Minimal Arch Linux configuration focused on a terminal-first workflow.
